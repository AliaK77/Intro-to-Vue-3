const app = Vue.createApp({ // keep { on this line not newline
        data(){
            return {
                product: 'Socks and turtles'
            } // end return
        } // end data()

    })  // end app createApp
